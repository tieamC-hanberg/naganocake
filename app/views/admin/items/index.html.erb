<div class='container'>
  <div class='row'>
    <div class='col-md-4 mt-4 mb-3'  style='display:flex; flex-wrap:wrap;'>
      <h2>商品一覧</h2>
      <div class="mt-3"><strong>(全<%= @items.count %>件)</strong></div>
    </div>
    <div class="ml-auto mt-4 mb-3">
      <p><%= link_to '新規登録',new_admin_item_path, class: "btn btn-outline-info" %></p>
    </div>
      <p><%= render 'index', items: @items %> </p>
  </div>
   <div style="margin:0 auto;">
    <%= paginate @items %>
   </div>
</div>
