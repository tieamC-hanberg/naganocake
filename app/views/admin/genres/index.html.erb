<div class="container">
  <div style="d-flex align-items-center height:100px;">
    <h4>
     ジャンル一覧/追加
    </h4>
  </div>
</div>
  <div class="container">
    <div class = row>
      <div class="col-lg-8">
        <%= render 'create', genre: @genre %>
      </div>
    </div>
        <div class="container">
    <div class = row>
      <div class="col-lg-5">
         <table class="table" style="font-size: 13pt; line-height: 100%;">
           <thead>
            <tr align="center">
             <th bgcolor="#f5f5f5">ジャンル</th>
             <th bgcolor="#f5f5f5"></th>
            </tr>
          </thead>
　        <tbody>
            <!--ジャンル名表示-->
            <% @genres.each do |genre| %>
              <tr><th><%= genre.name %></th><td><%= link_to "編集する", edit_admin_genre_path(genre.id), class: "btn btn-success btn-sm" %></td></tr>
　          <% end %>
          </tbody>
         </table>
      </div>
    </div>

